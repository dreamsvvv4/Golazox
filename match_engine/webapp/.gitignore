# Dependencias Node
node_modules/

# Variables de entorno (NUNCA subir al repo)
.env
.env.local
.env.production
.env.social

# Logs y lock files de PM2/YouTube
*.log
npm-debug.log*
logs/

# Videos generados (demasiado grandes para git)
videos/

# Cache de squads (datos regenerables con seed)
squads/.seed-progress.json

# Archivos de sistema
.DS_Store
Thumbs.db
desktop.ini

# Datos privados de usuarios (GDPR)
contact_messages.json
subscribers.json
.tiktok_verifier
.tiktok_verifier.tmp

# Scripts de desarrollo local (no van a producción)
dev/
